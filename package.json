{
  "name": "matcha-fullstack",
  "version": "1.0.0",
  "private": true,
  "description": "MatchA - Modern Japanese Artisan Streetwear & E-Commerce Full-Stack Platform (React 18, Express, MongoDB Atlas)",
  "scripts": {
    "build": "npm run build --prefix frontend",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "test": "node -r dotenv/config test_full_system.js dotenv_config_path=backend/.env",
    "seed": "npm run seed --prefix backend",
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "dotenv": "^17.4.2"
  }
}
